# 

```
$(cat /etc/mdjfkjsfkj) => "" => grep => full dictionary
$(cat /etc/passwd) => non-empty string => grep => empty dict

$(grep "^" /etc/natas_webpass/natas17)
\\
 \`---> If not matched, full dict.
  \
   `--> If matched, partial or none.
```
